import { render } from '@testing-library/react';
import { describe, it } from 'vitest';
import App from '@/App';
describe('App', () => {
  it('Renders app home page', () => {
    render(<App />);
    // agrs
    // actions
    // expecting
    // expect(
    //   screen.getByRole
    // )
  });
});
